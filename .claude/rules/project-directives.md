# Project Directives — permanent, always loaded
> 🇰🇷 이 프로젝트의 영구 지침. 이 파일은 `paths` 조건이 없으므로 **모든 세션, 모든 작업에서 항상 로드**되며, compact / clear / 새 세션 이후에도 디스크에서 다시 읽혀 절대 사라지지 않는다.
> `/setup` 또는 `/remember <지침>`이 여기에 기록한다. **≤ 40줄 유지** — 가득 차면 추가 전에 병합/가지치기부터 (guides/11).

## Directives

(none yet — add with `/remember <directive>`, or during `/setup`)

<!-- Format: numbered, imperative, ≤ 2 lines each. Examples:
1. All user-facing text ships in both Korean and English.
2. Never add a new dependency without a docs/DECISIONS.md entry.
3. Every new table gets RLS + at least one policy in the same migration.
-->
