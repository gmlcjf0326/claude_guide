---
description: Thorough session restore from disk — deeper than the automatic SessionStart injection
---

1. Read `docs/PROGRESS.md`, `docs/TODO.md`, the last 2 entries of `docs/SESSION_LOG.md`, and skim `docs/SPEC.md` + `docs/PLAN.md` headings.
2. Run `git log --oneline -5` and `git status` to cross-check disk state against reality.
3. Summarize in ≤ 8 lines: where the project stands, what is in flight, known blockers.
4. Freshness check: if `docs/CODEBASE_MAP.md` looks older than the code (spot-check 3 entries), flag it and suggest `/map`.
5. Propose the next task and wait for confirmation.
> 🇰🇷 디스크가 진실이다. 기억과 디스크가 다르면 디스크를 믿는다.
