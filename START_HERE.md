# START HERE — 처음 오신 분을 위한 완전 가이드
> 이 문서 하나만 따라 하면 30분 안에 COMPASS가 몸에 붙습니다. 전문 지식 불필요.
> (숙련자는 README.md로 바로 가셔도 됩니다.)

## 1. 이게 뭔가요? (3줄 요약)

Claude Code는 똑똑하지만 **기억이 세션마다 리셋되고, 요구를 대충 넘겨짚고, 검증 없이 "됐다"고 말하는** 약점이 있습니다. COMPASS는 그 약점을 막는 운영체계입니다 — 이해했는지 점수로 확인하고(/spec), 선택지를 비교해 기록하고(/blueprint), 검증 없인 완료를 못 하게 하고(/inspect), 모든 상태를 디스크에 남겨 **몇 달짜리 프로젝트도 세션이 바뀌어도 이어지게** 합니다.

## 2. 정직한 장단점 — 도입 전에 아세요

| 관점 | 얻는 것 | 치르는 것 |
|---|---|---|
| 품질 | 엉뚱한 결과물·미검증 "완료"·보안사고가 구조적으로 차단됨 | 작업마다 게이트 통과 시간 +수 분 |
| 장기전 | 세션이 바뀌어도 2분 안에 이어감 (백만 토큰 프로젝트 가능) | /checkpoint 습관이 필요함 (훅이 도와줌) |
| 토큰 | 규칙은 해당 파일을 만질 때만 로드 (미사용 스택 = 0) | 세션당 약 1.5~2.5K 토큰의 상시 오버헤드 (추정) |
| 학습 | 명령 5개만 알면 시작 가능 | 첫 주엔 오히려 느리게 느껴질 수 있음 — 정상입니다 |
| 자율성 | 시스템이 다음 할 일을 먼저 제안 | 가끔 잔소리처럼 느껴짐 (끄는 법 §7) |

**솔직한 결론**: 한나절짜리 일회성 스크립트엔 과합니다. 하지만 "만들다 만 프로젝트"가 하나라도 있다면, 첫 주의 학습 비용은 둘째 주에 회수됩니다.

## 3. 준비물 체크

- [ ] Claude Code v2.x 설치됨 (`claude --version`으로 확인)
- [ ] macOS/Linux면 그대로 OK · **Windows면 WSL 또는 Git Bash 필수** (훅이 bash라서)
- [ ] git 사용 가능
- [ ] (선택) Opus 접근 가능한 플랜 — 없어도 동작합니다, §6 참조

## 4. 설치 — 5분 (⚠️ 함정 1개 주의)

```bash
# 1) 압축 해제 후, 프로젝트 폴더에서:
cp -r /경로/COMPASS/. .        # ← 마지막의 ". ." 이 핵심!
```

> ⚠️ **가장 흔한 설치 사고**: `.claude` 폴더는 **숨김 폴더**라서 탐색기/Finder로 드래그 복사하면 빠지기 쉽습니다. 빠지면 훅·명령어·규칙 전부가 조용히 죽습니다. 위의 `cp -r 원본/. 대상` 명령(점 포함)은 숨김 파일까지 복사합니다. Windows CMD라면: `xcopy /경로\COMPASS . /E /H /Y`

```bash
# 2) 확인 — 이 두 개가 보여야 성공:
ls -a          # .claude 와 CLAUDE.md 가 목록에 있는가?

# 3) git 초기화 (이미 저장소면 생략)
git init && git add -A && git commit -m "chore: install COMPASS"
```

`.gitignore`에 추가: `.claude/settings.local.json` / `.env*` / `!.env.example`

## 5. 첫 실행 — 이런 화면이 뜹니다 (겁먹지 마세요)

`claude`를 실행하면 처음 한 번 이런 질문들이 나올 수 있습니다:

- **"이 프로젝트의 설정(settings.json)을 신뢰하시겠습니까?"** → 예. (방금 당신이 넣은 COMPASS 설정입니다)
- **훅 실행 승인 요청** → 예. (자동 포맷·비밀파일 차단 같은 안전장치입니다)

그리고 정상 설치라면 Claude의 **첫 응답에 이런 브리핑이 자동으로** 나타납니다:

```
프로젝트가 아직 프로파일링되지 않았네요 — /setup부터 실행하시길 제안합니다.
```

이게 보이면 성공입니다. 안 보이면 §7의 문제해결로.

## 6. Opus가 없는 플랜(Pro 등)이라면 — 3곳만 수정

1. `.claude/settings.json`에서 `"model": "opusplan",` 줄 삭제
2. `.claude/commands/spec.md` 와 `blueprint.md` 의 `model: opus` 줄 삭제
3. `.claude/agents/architect.md` 의 `model: opus` → `model: sonnet` 으로 변경

나머지 전부는 Sonnet만으로 동일하게 동작합니다.

## 7. 30분 연습 미션 — 몸으로 익히기 (강력 추천)

빈 연습 폴더에 설치 후, 아래를 그대로 따라 하세요. 실전 투입 전 시스템 감각이 잡힙니다.

| 단계 | 입력 | 성공하면 이렇게 보임 |
|---|---|---|
| 1 | `/setup` | 5~8개 질문 → 답하면 `docs/PROJECT.md`가 채워졌다고 보고 |
| 2 | `/healthcheck` | 훅·명령어·상태파일 점검표가 PASS 위주로 출력 |
| 3 | `/spec 방문자 카운터가 있는 자기소개 한 페이지` | 질문 몇 개 → "신뢰도 90%+" → SPEC 요약 → 승인 요청 |
| 4 | `승인. /blueprint` | 선택지 비교(또는 "경로가 하나뿐" 선언) → PLAN·TODO 생성 |
| 5 | `/next` | 작업 하나를 `[~]`로 잡고 구현 → 검증 출력까지 보여줌 |
| 6 | `/checkpoint` | TODO 정리·커밋·"다음 세션: ~부터" 한 줄 |
| 7 | **터미널을 완전히 껐다가** `claude` 재실행 | 첫 응답에 진행상황 요약 + "이어서 T-00X 할까요?" ← **이 순간이 COMPASS의 존재 이유입니다** |

## 8. 자주 겪는 문제 → 해결

| 증상 | 원인/해결 |
|---|---|
| `/setup` 쳐도 명령어 목록에 없음 | `.claude` 폴더 누락(§4 함정) — 복사 다시. `ls .claude/commands`로 14개 확인 |
| 훅이 아무 반응 없음 (Windows) | PowerShell/CMD에서 실행 중 — **WSL이나 Git Bash에서 `claude` 실행** |
| 세션 시작 브리핑이 안 뜸 | `/healthcheck` 실행 → 훅 등록 여부 진단. `docs/` 폴더 존재도 확인 |
| 영어로 대답함 | 첫 메시지에 "한국어로" 한 번, 또는 `/remember 모든 대화는 한국어로` — 이후 영구 |
| 비대화 경고(BLOAT)가 성가심 | `.claude/hooks/post-edit.sh`의 300/500 숫자를 조정하거나 해당 블록 삭제 |
| "in-progress 작업이 있다"며 종료를 막음 | 정상 동작(stop-gate). TODO의 `[~]`를 `[x]`나 `[!]사유`로 정리하면 통과 |
| `jq` 없다는 걱정 | 없어도 동작합니다(폴백 내장). 설치하면 조금 더 견고할 뿐 |
| 테스트 키를 .env에 넣어달랬는데 거부함 | 기본 잠금 상태입니다. `/secrets on` 한 번이면 열립니다 (끝나면 `/secrets off`) |

## 9. 학습 로드맵 — 한 번에 다 읽지 마세요

- **오늘**: 이 문서 + 연습 미션. 끝.
- **첫 주**: `guides/14-daily-playbook.md` (실전 대본 22개 — 뭘 타이핑하는지) 하나만.
- **필요할 때**: 장기 프로젝트 시작 → 07 · 도커 → 08 · 앱 수익화 → 13 · 공공기관 → 16. 나머지는 참고서처럼.
- **모르겠으면 그냥 물어보세요**: "지금 뭘 해야 해?" — 라우팅은 시스템이 압니다.

## 10. 업그레이드와 제거 (⚠️ 함정 2)

**업그레이드** — 새 버전 zip을 받았을 때 **통째로 덮어쓰면 절대 안 됩니다.** 다음은 당신의 데이터입니다:

```
보존 (덮어쓰기 금지):  docs/ 전체 · .claude/rules/project-directives.md
교체 (새 버전으로):    CLAUDE.md · guides/ · templates/ · .claude/의 나머지
```

```bash
# 안전 업그레이드 예시
cp -r 새버전/COMPASS/guides ./ && cp 새버전/COMPASS/CLAUDE.md ./
cp -r 새버전/COMPASS/templates ./
rsync -a --exclude 'rules/project-directives.md' 새버전/COMPASS/.claude/ .claude/
```

**제거** — `CLAUDE.md`, `.claude/`, `guides/`, `templates/` 삭제하면 원상복구. `docs/`는 당신의 프로젝트 기록이니 남겨두세요.

## 11. 커맨드 카드 (뜯어서 모니터에)

```
/setup       프로젝트 처음 1회 — 헌장+영구지침     /restore     디스크에서 심층 복원
/spec 목표   뭘 만들지 인터뷰 (코드 없음)          /map         코드 색인 생성/갱신
/blueprint   선택지 비교 → 계획+TODO              /advise 질문  Opus 아키텍트 소환
/next        다음 작업 1개 실행+검증               /remember 지침 영구 저장
/inspect     독립 리뷰 PASS 필수                  /improve     개선점 톱3 제안
/checkpoint  세이브 (끝낼 때·compact 전 필수)      /research 주제 조사→영구 문서화
/healthcheck 설치 자가진단                 /secrets on  테스트 키 .env 해제                        막히면: "지금 뭘 해야 해?"
```

**외울 필요 없습니다** — 자연어로 말하면 시스템이 해당 명령을 스스로 호출하거나 그 절차를 수행합니다 ("다음 거 하자", "저장하고 끝내자"면 충분). 그리고 반복하는 요청이 생기면 이렇게 말해보세요: `매일 아침 루틴을 /morning 명령으로 만들어줘` — 명령어 생성·수정도 대화로 됩니다 (내장 충돌은 /healthcheck가 감시).

즐거운 빌딩 되세요. 마찰이 느껴지는 지점은 버그가 아니라 `/improve`의 입력입니다 — 시스템이 당신에게 맞춰지는 과정입니다.
