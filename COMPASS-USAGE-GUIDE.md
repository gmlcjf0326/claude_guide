# COMPASS 사용설명서 (v1.13.0 · 완전판 한 장 요약)
> 이 문서 하나로 시스템 전체의 사용법을 커버합니다. 작업하며 옆에 펴놓는 매뉴얼.
> 온보딩은 `START_HERE.md`, 세부 근거는 `guides/00–16`, 이 문서는 "어떻게 쓰는가"의 전부.

---

## 1. 이게 무엇인가 (30초)

COMPASS는 Claude Code 위에서 도는 **에이전트 코딩 운영체계**입니다. Claude Code의 3대 약점 — ① 요구를 넘겨짚고 ② 검증 없이 "됐다"고 하고 ③ 세션이 바뀌면 잊는 것 — 을 구조로 막습니다. 핵심 철학 한 줄: **"디스크가 진실, 컨텍스트는 캐시"** — 모든 상태는 `docs/` 파일에 살고, 세션·압축·크래시가 무엇을 지워도 디스크에서 되살아납니다.

---

## 2. 설치 (5분) — 함정 2개만 조심

```bash
# 프로젝트 폴더에서 (마지막 ". ." 이 숨김폴더까지 복사하는 핵심!)
cp -r /경로/COMPASS/. .
ls -a                      # .claude 와 CLAUDE.md 가 보이면 성공
git init && git add -A && git commit -m "chore: install COMPASS"
```

- ⚠️ **함정 1**: `.claude`는 숨김폴더 — 탐색기 드래그 복사에서 빠지면 시스템 절반이 조용히 죽음. 위 명령으로 복사.
- ⚠️ **함정 2 (업그레이드 시)**: 새 버전을 통째로 덮어쓰지 말 것. **보존**: `docs/` 전체 + `.claude/rules/project-directives.md` (당신의 데이터). **교체**: 나머지 전부.
- `.gitignore`: `.claude/settings.local.json` / `.claude/secrets.unlock` / `.env*` / `!.env.example`
- Opus 없는 플랜: `settings.json`의 `"model": "opusplan"` 줄 + `spec.md`·`blueprint.md`·`architect.md`의 model 지정만 삭제 → 전부 Sonnet으로 동작.

---

## 3. 첫 시작 순서 (Day-0)

```
claude 실행 → /effort max → /setup → /healthcheck → /spec 첫 목표
```

- `/setup` (프로젝트당 1회): 5~8문 인터뷰로 **프로젝트 헌장**(docs/PROJECT.md) 작성 — 단계·비즈니스모델·품질편향·비타협원칙. 여기서 답한 **영구 지침**은 project-directives.md에 박혀 영원히 로드됩니다. 미리 연구한 문서가 있으면 `docs/inputs/`에 넣고 실행 — 먼저 전부 읽고, 문서가 답한 건 안 묻습니다.
- `/healthcheck`: 훅·명령어 14개·상태파일·게이트를 자가진단. 뭔가 이상하면 언제든 이것부터.

---

## 4. 황금 경로 — 모든 작업의 뼈대

```
/spec 목표  →  /blueprint  →  /next (반복)  →  /inspect  →  /checkpoint
 이해 90%↑      옵션 비교        한 작업씩         독립 리뷰      세이브+커밋
```

내부 상태머신: RESUME → UNDERSTAND(신뢰도 ≥90% 게이트) → OPTIONS → PLAN → BUILD → VERIFY → CHECKPOINT. 게이트를 건너뛰지 않는 것이 규율의 전부입니다.

**규모별 경량화** — 사소한 수정(오타·한 줄): 그냥 말하면 됨(4→6단계만). 몇 시간짜리: SPEC 문서 생략, "TODO에 완료기준 2~3개 적고 진행해". 실험(스파이크): `[spike]` 태그, 코드는 버리고 배움만 DECISIONS에.

---

## 5. 명령어 14개 완전 레퍼런스

| 명령 | 언제 | 하는 일 |
|---|---|---|
| `/setup` | 프로젝트 처음 1회·피벗 후 | 헌장+영구지침+맞춤조준. inputs/ 문서 흡수 |
| `/spec 목표` | 반나절↑ 새 작업 | 인터뷰(라운드당 3~5문, A/B/C 선택지) → 신뢰도 90% → SPEC. **코드 금지** |
| `/blueprint` | 방법을 정할 때 | 결정마다 효율형/효과형/균형형 비교 → PLAN+TODO. 되돌리기 힘든 건 아키텍트 자문 |
| `/next` | 계획된 다음 일 | 작업 1개: `[~]`표시→구현→게이트→`[x]`→상태갱신. 하루 최다 사용 |
| `/inspect` | "다 됐다" 직전 | 새 눈의 리뷰어가 diff를 계획·DoD 대비 PASS/FAIL. FAIL은 협상 불가 |
| `/checkpoint` | 세션 끝·compact/clear 전 **필수** | TODO 정직화+PROGRESS+커밋+푸시+"다음: ~" |
| `/restore` | 이어 시작·오래 비움 | 디스크 심층 복원 + git 교차확인 + 지도 신선도 |
| `/map` | 코드 찾기 어려울 때 | 결정 수준 색인(CODEBASE_MAP) 생성/갱신 |
| `/advise 질문` | 설계 갈림길 | Opus 아키텍트: 판정/리스크/놓친 대안 |
| `/remember 지침` | 영구 규칙 생겼을 때 | 성격 분류 후 올바른 영구 저장소에 기록 |
| `/improve` | 마일스톤·답답할 때 | 개선 톱3를 옵션으로. 반복 실수는 규칙/훅으로 승격 |
| `/research 주제` | 최신정보·근거 필요 | 소스 사다리로 조사 → `docs/research/`에 영구 저장 |
| `/secrets on\|off` | 테스트 키 다룰 때 | .env 잠금 해제/재잠금 (§10) |
| `/healthcheck` | 뭔가 이상할 때 | 설치 전체 자가진단 + 명령 섀도 감지 |

> **외울 필요 없음**: "다음 거 하자"≈/next, "저장하고 끝내"≈/checkpoint — 자연어면 시스템이 알아서 해당 명령을 호출합니다. 막히면 그냥 물으세요: "지금 뭘 해야 해?"

---

## 6. 하루의 리듬 (프롬프트 20개의 해부)

```
열기:  그냥 시작 — 코파일럿이 상태를 주입하고 다음 수를 먼저 제안
루프:  /next → [Claude 작업+증거] → 자연어 피드백("좋다"/"버튼은 오른쪽"/에러 붙여넣기)
중간:  컨텍스트 ~60% 또는 카운터 경고 → /checkpoint → /compact
닫기:  /checkpoint  (페이즈가 끝났으면 → /clear 로 다음 세션 신선하게)
```

전형적 하루 20프롬프트 = 명령 3개 + 인터뷰 답 6개("1-A, 2는 추천해줘") + 자연어 반응 8개 + 교정 2개. **정성 들일 건 /spec 첫 설명뿐** — 목표·상황·제약·모르는 것 4가지만. 당신 주의력의 3대 레버리지: ① 계획 승인 때 옵션 정독 2분 ② 돈·인증·삭제 경로의 diff 직접 확인 ③ 체크포인트 때 TODO 정직성 훑기.

---

## 7. 자동으로 돌아가는 것들 (손댈 필요 없음)

**훅 5종 (법 — 컨텍스트 밖에서 실행, compact도 못 지움)**
- guard-secrets: 비밀파일 접근 차단 (§10의 해제 모드 존중)
- post-edit: 저장 시 자동 포맷 + 비대화 경고(300/500줄) + 무보호작업 카운터(25/50/75 편집 시 체크포인트 제안)
- session-resume: 세션 시작마다 헌장·진행상황·열린작업 주입 + **다음 수 1개 제안**
- stop-gate: `[~]` 방치 상태로 종료 차단 (정리하면 통과)
- statusline: 터미널 하단 상시 — `🧭 · phase · 열린/진행 · 편집수 (· UNLOCKED)`

**규칙 자동 로드**: TS/Rust/Python/Java/Postgres/서버리스/AI/Docker/디자인 규칙이 해당 파일을 만질 때만 로드 (미사용 = 0토큰). 당신의 영구 지침(project-directives.md)만 항상 로드.

---

## 8. 영속 계층 — 무엇이 살아남나

| 층 | 위치 | 생존 방식 |
|---|---|---|
| 절대 규칙 | 훅 (settings.json) | 컨텍스트 무관 — 항상 실행 |
| 헌법·컨벤션·**영구지침** | CLAUDE.md · rules/ | 매 세션 디스크에서 재로드 |
| 프로젝트 정체성 | docs/PROJECT.md | 시작 훅이 자동 주입 |
| 작업 상태 | docs/ 8파일 (SPEC·PLAN·TODO·PROGRESS·MAP·DECISIONS·LOG·BACKLOG) | 체크포인트로 갱신, 자동 주입 |
| 조사 지식 | docs/research/ | 한 번 조사, 영원히 재사용 |
| ❌ 대화 속 약속 | 컨텍스트에만 | **compact가 지움 → /remember로 승격할 것** |

---

## 9. 장기 프로젝트 (100K~1M+ 토큰) 프로토콜

- **60% 룰**: 컨텍스트 60%쯤에서 `/checkpoint` → `/compact` (자동압축 임박까지 기다리면 열화된 요약이 됨). 압축과 기억이 다르면 **디스크가 이깁니다**.
- **페이즈-퍼-세션**: 한 단계 끝 = `/checkpoint` → `/clear` — 신선한 컨텍스트 + 좋은 문서 > 지친 긴 히스토리.
- **드리프트 의식**: 페이즈 경계마다 SPEC 재독 → 일치/스펙갱신/코드교정 셋 중 하나를 소리내어. 침묵만 금지.
- **건강 지표 = Resume Test**: 새 세션에 "계속해"만 쳤을 때 2분 내 올바른 다음 행동이 나오는가.

---

## 10. 보안 — 기본 잠금 + 테스트 해제

기본: `.env`·키파일은 읽기/쓰기 차단(훅). **테스트 키를 채팅으로 줄 때**:

```
/secrets on   → .gitignore 확인/보강 후 잠금해제 (세션 배너+상태줄에 🔓 상시 표시)
              → 키를 .env에 기록 — 값은 절대 다시 출력 안 함(이름으로만 확인)
              → 키가 필요한 테스트를 실행해 동작 증명
/secrets off  → 재잠금 (7일 넘게 켜두면 /healthcheck가 지적)
```
어떤 모드에서도 불변: 값 에코 금지 · env 커밋 금지 · `.pem`/인증서/개인키 영구 차단.

---

## 11. 디자인 작업 — "AI 티" 안 나게

- **에셋은 첫 작업의 일부**: 아이콘은 lucide-react(이모지 금지), 폰트는 한국어 제품이면 Pretendard Variable(레시피: 가이드12 §7), 이미지는 프로토=picsum/프로덕션=다운로드+CREDITS.md. 시스템폰트+이모지 화면은 초안이 아니라 리뷰 차단 대상.
- **인라인 스타일 금지** (효율 원칙): 태그별 `style=` 금지 — 예외 3개만(JS 계산 동적값 / CSS변수 주입 / 인스턴스별 런타임값). 구조는 `tokens→base→components→utilities` + `@layer`.
- **공공기관 프로젝트**: `templates/design/`의 KRDS 토큰이 출발점 — 모든 색 조합 실측 검증(본문 16.18:1 등), **매직넘버 법칙**(토큰 등급차 ≥50 ⇒ 4.5:1)으로 암산 준수. 정적 사이트=css 3개 링크, React=tailwind 브릿지 import. 상황별(중앙부처/지자체/대시보드) 응용: 가이드16.
- 취향 전달의 정석: 좋아하는 화면 **캡쳐 2~3장**을 /spec 때 주기 — 말보다 대역폭이 큼. 캡쳐 재현이 필요하면 proposal-deck-builder 스킬의 재현 프로토콜 방식(픽셀 색 샘플링+렌더비교 루프)을 참고.

---

## 12. 시스템을 내 것으로 만들기

- `/remember 한 줄` — 오늘 배운 규칙 영구화 (분류는 시스템이).
- 반복 요청 → **명령으로 승격**: "매일 아침 루틴을 /morning 명령으로 만들어줘" (내장 충돌은 /healthcheck가 감시).
- 같은 실수 2회 → `/improve`가 **승격 사다리**(관찰→규칙→체크리스트→훅) 제안 — 시스템이 스스로 배웁니다.
- 임계값 조정: 비대화 300/500은 `post-edit.sh`, 카운터 25/50/75도 같은 파일. 훅이 싫으면 settings.json에서 해당 블록 삭제(문서 규칙은 유지됨).

---

## 13. 문제해결 초압축

| 증상 | 해결 |
|---|---|
| 명령어가 목록에 없음 | `.claude` 누락(§2 함정1) 또는 새 내장과 충돌 → `/healthcheck` |
| 훅 무반응 (Windows) | WSL/Git Bash에서 실행 |
| 영어로 답함 | `/remember 모든 대화는 한국어로` 한 번이면 영구 |
| 종료를 막음 | 정상(stop-gate) — TODO의 `[~]` 정리 |
| .env 접근 거부 | 정상(기본잠금) — 테스트면 `/secrets on` |
| 뭔가 잊은 듯 | `/restore` — 디스크가 진실 |

---

## 14. 파일 지도 (한 화면)

```
CLAUDE.md               헌법: 상태머신·12규칙·라우팅 (매 세션 로드)
START_HERE.md           초보 온보딩 / README.md 전체 참조
.claude/
  settings.json         모델·권한·훅 배선        hooks/ 자동장치 5종
  commands/ 14개         skills/ 7개(인터뷰·트레이드오프·색인·비대화·장기전·DoD·리서치)
  agents/  architect(Opus)·code-reviewer·explorer
  rules/   스택별 자동로드 + project-directives.md(항상, 당신의 영구지침)
docs/                   ★ 당신의 상태 — 업그레이드 시 보존!
  PROJECT SPEC PLAN TODO PROGRESS CODEBASE_MAP DECISIONS SESSION_LOG BACKLOG
  inputs/(사전연구 투입구) research/(조사 영구보관)
guides/00–16            심층 근거 (07 장기전 · 08 도커 · 13 앱수익화 · 14 실전대본 · 16 공공디자인)
templates/              문서 템플릿 · docker/ · ci/ · design/(KRDS 토큰+데모)
```

---

## 15. 정직한 한계 (알고 쓰기)

① 명령은 100% 발동, 스킬 자동발동은 확률 — 확실함이 필요하면 명령을 부르세요. ② 훅은 표준 도구 경로를 지킴 — "예쁨" 자체는 강제 불가, 취향은 레퍼런스로. ③ 인터뷰 품질 = 입력 품질 — 모르면 "모르니 추천해줘"가 정답. ④ 첫 주는 느리게 느껴짐 — 둘째 주에 회수됩니다.

**요약의 요약**: `/setup` 한 번 → `/spec`으로 시작 → `/next`로 굴리고 → `/checkpoint`로 끝낸다. 나머지는 자연어로 말하면 시스템이 안다. 🧭
